import React from 'react';

const NotFoundPage = () => {
  return (
    <div
      style={{
        height: 'calc(100vh - 86px)',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}
    >
      NotFoundPage...
    </div>
  );
};

export default NotFoundPage;
